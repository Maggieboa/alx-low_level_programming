#include<stdio.h>
/**
 *main - function to print out the program name
 *@argc: to print out the cmd line arguments entered
 *@argv: to print an array for the number of args entered
 *Return: 0 for success
 *
 **/
int main(int __attribute__((__unused__)) argc, char *argv[])
{

printf("%s\n", argv[0]);
return (0);
}

#include<stdlib.h>
#include<stdio.h>
/**
 *main-the main function of the program
 *argc:placeholder variable for calc. the number of functions;
 *argv:placeholder variable
 *Return:0 for success
 *
 **/
int main(int __attribute__((unused)) argc, char *argv[])
{
int i;
i = 0;

printf("%s\n", argv[i]);
return (0);
}

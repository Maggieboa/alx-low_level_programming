#include <stdio.h>
/**
 *main - start of our program
 *
 *
 *
 *
 *Return: returns results after for loop statements
 */
int main(void)
{

	char alph;

	while (alph != 'e' || alph != 'q')
	{
		putchar(alph);
		alph++;
	}
		putchar('\n');
	return (0);
}
